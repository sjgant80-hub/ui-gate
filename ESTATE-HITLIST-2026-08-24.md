# ui-gate estate hit-list

Scanned 440 live pages · 394 clean · 46 flagged

## By defect kind

- **alarm-word** — 23 page(s): cassietorusbtc135solver, datascope, fall-sales-marketing, fall127agents, fallcall, fallconcierge, fallconsensus, fallcore, fallcore-factory, fallcube-api, fallforce, fallforensics, fallgo, fallgrade, falllead, fallmesh, fallreach, fallscout, fallsignal, forge-upgrade, gymos, konomi-rubric, konomi-swarm
- **undefined-css-var** — 16 page(s): fall-substrate, fallaccount-trades, fallcarousel, fallestate, fallestate-us, fallforge, fallhub, fallmarket, fallrecall, fallseed-village, fallworld, gymos, konomi-redteam, konomi-regress, konomi-rubric, smbaios
- **dead-link** — 9 page(s): Eden, datascope, fall-kqtt-bridge, fall-substrate, fallcall, fallharbor, fallscout, nhs-reinjection, smbaios
- **dead-control** — 5 page(s): fallbooks-us, fallbookspaper, fallclaimonboard, falllegalonboard, fallseed-agents

## Every flagged page (worst-first)

### fallmarket  —  0🔴 7🟠 0🟡
  https://sjgant80-hub.github.io/fallmarket/
  - 🟠 [undefined-css-var] CSS var --brass is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --ink is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --dim is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --panel is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --border is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --radius is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --green is used but never defined — it silently falls back to nothing

### smbaios  —  0🔴 3🟠 3🟡
  https://sjgant80-hub.github.io/smbaios/
  - 🟠 [undefined-css-var] CSS var --color-user-mention-fg is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --bgColor-attention-muted is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --color-attention-subtle is used but never defined — it silently falls back to nothing
  - 🟡 [dead-link] a link labelled "Reload" points nowhere (href="(empty)")
  - 🟡 [dead-link] a link labelled "Reload" points nowhere (href="(empty)")
  - 🟡 [dead-link] a link labelled "Reload" points nowhere (href="(empty)")

### fallcall  —  0🔴 1🟠 4🟡
  https://sjgant80-hub.github.io/fallcall/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)
  - 🟡 [dead-link] a link labelled "FallCall" points nowhere (href="#")
  - 🟡 [dead-link] a link labelled "API Docs" points nowhere (href="#")
  - 🟡 [dead-link] a link labelled "Privacy" points nowhere (href="#")
  - 🟡 [dead-link] a link labelled "Terms" points nowhere (href="#")

### fallclaimonboard  —  0🔴 5🟠 0🟡
  https://sjgant80-hub.github.io/fallclaimonboard/
  - 🟠 [dead-control] a control calls openModal() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)
  - 🟠 [dead-control] a control calls newClient() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)
  - 🟠 [dead-control] a control calls openClient() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)
  - 🟠 [dead-control] a control calls newComplaint() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)
  - 🟠 [dead-control] a control calls editComplaint() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)

### fall-substrate  —  0🔴 1🟠 3🟡
  https://sjgant80-hub.github.io/fall-substrate/
  - 🟠 [undefined-css-var] CSS var --green is used but never defined — it silently falls back to nothing
  - 🟡 [dead-link] a link labelled "Download HTML" points nowhere (href="#")
  - 🟡 [dead-link] a link labelled "Copy &nbsp;gh repo create&nbsp; command" points nowhere (href="#")
  - 🟡 [dead-link] a link labelled "View fake live URL" points nowhere (href="#")

### fallestate  —  0🔴 4🟠 0🟡
  https://sjgant80-hub.github.io/fallestate/
  - 🟠 [undefined-css-var] CSS var --mono is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --sage-bright is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --sage is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --dim is used but never defined — it silently falls back to nothing

### fallestate-us  —  0🔴 4🟠 0🟡
  https://sjgant80-hub.github.io/fallestate-us/
  - 🟠 [undefined-css-var] CSS var --mono is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --sage-bright is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --sage is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --dim is used but never defined — it silently falls back to nothing

### fallforge  —  0🔴 3🟠 0🟡
  https://sjgant80-hub.github.io/fallforge/
  - 🟠 [undefined-css-var] CSS var --forge is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --good is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --cream is used but never defined — it silently falls back to nothing

### datascope  —  0🔴 1🟠 1🟡
  https://sjgant80-hub.github.io/datascope/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)
  - 🟡 [dead-link] a link labelled "DataScope ◊" points nowhere (href="#")

### fallcarousel  —  0🔴 2🟠 0🟡
  https://sjgant80-hub.github.io/fallcarousel/
  - 🟠 [undefined-css-var] CSS var --accent-bg is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --accent-fg is used but never defined — it silently falls back to nothing

### fallhub  —  0🔴 2🟠 0🟡
  https://sjgant80-hub.github.io/fallhub/
  - 🟠 [undefined-css-var] CSS var --dim is used but never defined — it silently falls back to nothing
  - 🟠 [undefined-css-var] CSS var --brass2 is used but never defined — it silently falls back to nothing

### falllegalonboard  —  0🔴 2🟠 0🟡
  https://sjgant80-hub.github.io/falllegalonboard/
  - 🟠 [dead-control] a control calls openModal() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)
  - 🟠 [dead-control] a control calls openClient() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)

### fallscout  —  0🔴 1🟠 1🟡
  https://sjgant80-hub.github.io/fallscout/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)
  - 🟡 [dead-link] a link labelled "Scout" points nowhere (href="#")

### gymos  —  0🔴 2🟠 0🟡
  https://sjgant80-hub.github.io/gymos/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)
  - 🟠 [undefined-css-var] CSS var --bg-alt is used but never defined — it silently falls back to nothing

### konomi-rubric  —  0🔴 2🟠 0🟡
  https://sjgant80-hub.github.io/konomi-rubric/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge)
  - 🟠 [undefined-css-var] CSS var --a is used but never defined — it silently falls back to nothing

### cassietorusbtc135solver  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/cassietorusbtc135solver/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fall-sales-marketing  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fall-sales-marketing/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fall127agents  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fall127agents/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallaccount-trades  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallaccount-trades/
  - 🟠 [undefined-css-var] CSS var --border is used but never defined — it silently falls back to nothing

### fallbooks-us  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallbooks-us/
  - 🟠 [dead-control] a control calls taxPeriod() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)

### fallbookspaper  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallbookspaper/
  - 🟠 [dead-control] a control calls openModal() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)

### fallconcierge  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallconcierge/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallconsensus  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallconsensus/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallcore  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallcore/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallcore-factory  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallcore-factory/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallcube-api  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallcube-api/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallforce  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallforce/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallforensics  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallforensics/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallgo  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallgo/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallgrade  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallgrade/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### falllead  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/falllead/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallmesh  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallmesh/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallreach  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallreach/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallrecall  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallrecall/
  - 🟠 [undefined-css-var] CSS var --gold is used but never defined — it silently falls back to nothing

### fallseed-agents  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallseed-agents/
  - 🟠 [dead-control] a control calls openAgentTaskRunner() but no INLINE script defines it — likely a dead button (unless it lives in an external .js)

### fallseed-village  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallseed-village/
  - 🟠 [undefined-css-var] CSS var --gold is used but never defined — it silently falls back to nothing

### fallsignal  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallsignal/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallworld  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/fallworld/
  - 🟠 [undefined-css-var] CSS var --edge-soft is used but never defined — it silently falls back to nothing

### forge-upgrade  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/forge-upgrade/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### konomi-redteam  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/konomi-redteam/
  - 🟠 [undefined-css-var] CSS var --p is used but never defined — it silently falls back to nothing

### konomi-regress  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/konomi-regress/
  - 🟠 [undefined-css-var] CSS var --a is used but never defined — it silently falls back to nothing

### konomi-swarm  —  0🔴 1🟠 0🟡
  https://sjgant80-hub.github.io/konomi-swarm/
  - 🟠 [alarm-word] "DEAD" shown to a user reads as broken (e.g. an unlicensed-tier badge) (assigned to the DOM in a script)

### fallharbor  —  0🔴 0🟠 2🟡
  https://sjgant80-hub.github.io/fallharbor/
  - 🟡 [dead-link] a link labelled "${esc(f)}" points nowhere (href="#")
  - 🟡 [dead-link] a link labelled "${esc(t.forks_from)}" points nowhere (href="#")

### Eden  —  0🔴 0🟠 1🟡
  https://sjgant80-hub.github.io/Eden/
  - 🟡 [dead-link] a link labelled "🔑" points nowhere (href="#")

### fall-kqtt-bridge  —  0🔴 0🟠 1🟡
  https://sjgant80-hub.github.io/fall-kqtt-bridge/
  - 🟡 [dead-link] a link labelled "this URL" points nowhere (href="(empty)")

### nhs-reinjection  —  0🔴 0🟠 1🟡
  https://sjgant80-hub.github.io/nhs-reinjection/
  - 🟡 [dead-link] a link labelled "Open in email" points nowhere (href="#")

